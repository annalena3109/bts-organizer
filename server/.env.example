# Back-to-School Backend Environment Variables
PORT=5000
NODE_ENV=development
JWT_SECRET=bts_cozy_secret_key_change_in_prod

# PostgreSQL Connection String (Optional for local fallback; Required on Render)
# Format: postgresql://<user>:<password>@<host>:<port>/<database>
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/backtoschool
